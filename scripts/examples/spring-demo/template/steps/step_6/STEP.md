# Visualise

Render a confusion matrix and per-feature distribution panels into `confusion.png` and `features.png`. Depends on `step_4`.

This step exists for the Spring animation demo — it is not intended to be executed by an agent.
