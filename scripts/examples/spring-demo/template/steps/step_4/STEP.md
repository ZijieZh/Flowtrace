# Analyze

Compute descriptive statistics by joining `clean.csv` with the validated `schema.json`, write `stats.json` and a one-page `summary.md`. Depends on `step_2` and `step_3`.

This step exists for the Spring animation demo — it is not intended to be executed by an agent.
