# Validate

Sanity-check the column types, ranges, and distribution shapes of `raw.csv` and write a `schema.json` describing the dataset. Depends on `step_1`.

This step exists for the Spring animation demo — it is not intended to be executed by an agent.
