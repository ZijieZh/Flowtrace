# Report

Compose the final HTML report from `metrics.json` and the visualisations. Write `report.html`. Depends on `step_5` and `step_6`.

This step exists for the Spring animation demo — it is not intended to be executed by an agent.
