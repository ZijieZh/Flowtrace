# Model

Train a baseline classifier on `stats.json` features and record cross-validated metrics. Write `model.pkl` and `metrics.json`. Depends on `step_4`.

This step exists for the Spring animation demo — it is not intended to be executed by an agent.
