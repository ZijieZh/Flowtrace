# Clean

Drop missing values from `raw.csv`, normalise the column schema, and write `clean.csv` plus a short `clean_log.txt`. Depends on `step_1`.

This step exists for the Spring animation demo — it is not intended to be executed by an agent.
